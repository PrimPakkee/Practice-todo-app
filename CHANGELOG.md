# 更新日志

本文档用于记录 Practice Todo App / TaskFlow AI Practice 的版本更新。

## v0.1.6

### 本次更新概述

完善前端核心任务交互，新增任务编辑、任务详情、删除确认和复制任务能力，继续保持 localStorage 单机版。

### 新增任务编辑能力

- 每个任务卡片新增编辑入口。
- 支持修改标题、状态、优先级、截止日期、标签和备注。
- 保存编辑会更新 `updatedAt`。
- 状态改为已完成时会设置 `completedAt`。
- 已完成任务重新打开时会清空 `completedAt`。
- Tasks 表单支持新建 / 编辑模式切换，并提供取消编辑。

### 新增任务详情弹窗 / 抽屉

- 每个任务卡片新增详情入口。
- 详情中展示标题、状态、优先级、截止日期、标签、完整备注、创建时间、更新时间和完成时间。
- 详情中支持编辑、复制、删除和关闭。

### 新增删除确认

- 删除任务前会打开自定义确认弹窗。
- 点击确认删除后才会真正删除任务。
- 点击取消会关闭弹窗，不使用浏览器 `window.confirm`。

### 新增任务复制

- 支持复制已有任务。
- 新任务标题自动添加“副本 -”前缀。
- 复制后的任务状态默认为待处理，并重新生成 id、createdAt 和 updatedAt。

### 优化任务卡片信息

- 任务卡片展示创建时间、更新时间和完成时间。
- 截止日期根据当前日期展示已逾期、今天截止或具体截止日期。
- Dashboard、Tasks、Timeline、Calendar 中的任务交互更统一，任务点击优先进入详情。

### 下一步计划

- 增加任务排序能力。
- 增加批量操作和清空已完成任务。
- 增加导入 / 导出本地数据功能。
- 继续优化移动端任务编辑体验。

## v0.1.5

### 本次更新概述

将 Todo 数据升级为更完整的任务对象，并增强 Tasks 页面表单与任务卡片展示，继续保持纯前端 localStorage 单机版。

### 验收修复

- 同步更新 README 文档，补充 v0.1.5 当前能力和版本规划。
- Calendar 改为根据当前月份真实天数生成日期格子。
- 任务卡片状态按钮改为更明确的文案：开始推进、标记完成、重新打开。
- 已完成任务会展示完成时间。
- 增强 localStorage 数据容错，非法任务、非法状态、非法优先级和异常标签数据都会安全回退。

### Calendar 验收修复

- 增加年份 / 月份选择。
- 增加上个月 / 下个月 / 回到今天操作。
- 增加周一到周日表头。
- 使用真实星期对齐日期。
- 将 dueDate 任务显示到对应日期格子。
- 增加未设置截止日期任务区域。

### 新增任务字段

- 新增 `title` 作为任务标题。
- 新增 `status`，支持待处理、进行中、已完成。
- 新增 `priority`，支持低、中、高优先级。
- 新增 `dueDate` 截止日期。
- 新增 `tags` 标签数组，支持逗号分隔输入。
- 新增 `note` 任务备注。
- 新增 `createdAt`、`updatedAt`、`completedAt` 时间字段。
- 兼容旧版 `{ id, text, completed }` 任务数据，会自动迁移为新结构。

### Tasks 页面增强

- 增加完整任务表单：标题、优先级、状态、截止日期、标签、备注。
- 增加按状态筛选：全部、待处理、进行中、已完成。
- 增加按优先级筛选：全部、高、中、低。
- 任务卡片展示标题、状态、优先级、截止日期、标签、备注摘要、创建时间。
- 任务卡片支持删除和状态切换。

### Dashboard 调整

- Dashboard 继续作为总览页，不承担完整任务管理职责。
- 统计卡片展示总任务数、已完成、进行中、未完成、完成率。
- 重点任务优先展示高优先级、未完成、有截止日期的任务。
- 快速添加任务保留为简化输入，只填写标题，其余字段使用默认值。

### Timeline / Calendar / Analytics 初步联动

- Timeline 按待处理、进行中、已完成分组展示真实任务。
- Timeline 中有截止日期的任务会显示截止日期。
- Calendar 保留月历网格，并在对应日期显示有 dueDate 的任务。
- Calendar 增加本月任务区域。
- Analytics 使用真实任务数据展示总数、已完成、进行中、高优先级和完成率。

### 下一步计划

- 增加任务编辑功能。
- 增加任务排序和更细的筛选条件。
- 优化 Calendar 的月份切换。
- 增加导入 / 导出 / 清空本地数据功能。

## v0.1.4

### 本次更新概述

围绕产品结构和基础体验做整理，让 Dashboard 更像总览页，让 Tasks 承担完整任务管理职责，并优化 Topbar 与搜索框交互。

### 验收修复

- 优化顶部搜索框交互：点击或输入时自动进入 Tasks 页面，并继续使用搜索词过滤任务。
- 清理 `src/index.css` 中残留的 Vite 默认模板样式，避免影响整体布局。
- 为 localStorage 读取增加 JSON 解析容错，数据损坏时回退为空任务列表。
- 将 Dashboard 中容易误导的“今日任务”文案调整为“重点任务”。

### 修复的问题

- 修复搜索框左侧“搜索”文字显示不美观的问题，改为更简洁的搜索符号。
- 修复 Topbar“新建任务”按钮直接尝试添加空任务的不自然逻辑。
- 减少 Dashboard 与 Tasks 页面职责重复。
- 删除 Analytics 页面中没有实际意义的随机效率评分。

### 页面职责调整

- Dashboard 页面聚焦总览：统计卡片、今日重点任务、快速添加任务、工作流概览。
- Tasks 页面负责完整任务管理：搜索、筛选、添加、删除、标记完成。
- Timeline 页面改为按待处理、已完成、最近创建展示，并说明后续会接入截止日期。
- Calendar 页面保留月历骨架，并说明后续会根据 dueDate 显示任务。
- Analytics 页面只展示基于真实任务数据计算的统计。
- Settings 页面展示 localStorage、版本信息和后续设置能力。

### 交互优化

- Sidebar 页面切换体验保持不变，并继续显示当前页面高亮。
- Topbar 标题和说明根据当前页面保持清晰。
- 点击 Topbar“新建任务”会切换到 Tasks 页面，并尝试聚焦任务输入框。
- Tasks 页面新增全部 / 未完成 / 已完成的真实筛选逻辑。

### 下一步计划

- 为任务增加优先级字段。
- 为任务增加截止日期 dueDate 字段。
- 让 Timeline 和 Calendar 接入真实日期逻辑。
- 增加导入、导出、清空数据和主题切换功能。

## v0.1.3

### 本次更新概述

将静态 Dashboard 原型升级为多页面前端骨架，使用 React `useState` 实现 Sidebar 菜单切换，不引入 React Router。

### 新增功能

- Sidebar 菜单支持点击切换页面。
- 当前选中的 Sidebar 菜单会高亮显示。
- Topbar 标题会根据当前页面自动变化。
- 新增 Tasks、Timeline、Calendar、Analytics、Settings 页面骨架。
- 保留 Dashboard 页面中的任务统计、任务输入区、任务列表和工作流概览。

### 界面更新

- 继续保留深色科技感、蓝紫渐变、毛玻璃卡片和网格背景。
- 新增任务管理页工具栏、筛选按钮占位和完整任务列表区域。
- 新增时间线分组卡片，包含“今天 / 本周 / 已完成”占位。
- 新增月历网格 UI 骨架。
- 新增统计分析图表占位和设置页信息区块。

### 技术更新

- 使用 `activePage` 状态管理当前页面。
- 使用条件渲染展示不同页面内容。
- 继续使用 React 状态管理 Todo 数据。
- 继续使用 localStorage 保存任务。
- 未添加后端、数据库、登录系统或复杂第三方 UI 库。

### 下一步计划

- 为 Tasks 页面增加真实筛选逻辑。
- 为任务增加优先级和截止日期字段。
- 让 Timeline 和 Calendar 使用真实任务日期。
- 增加数据导出和导入功能。

## v0.1.2

### 更新内容

- 将基础 Todo 页面优化为深色科技感界面。
- 增加蓝紫渐变、网格背景、毛玻璃卡片和柔和发光效果。
- 优化输入框、按钮、任务列表和完成状态的视觉表现。

## v0.1.1

### 更新内容

- 实现基础 Todo List。
- 支持添加任务。
- 支持删除任务。
- 支持标记任务完成 / 未完成。
- 使用 localStorage 保存任务，刷新页面后任务仍保留。

## v0.1.0

### 更新内容

- 初始化 React + Vite 项目。
- 保留基础项目结构和开发脚本。

## 下一步计划

- 升级为成熟 Dashboard 布局。
- 增加 Sidebar / Topbar。
- 增加任务优先级、截止日期、搜索和筛选。
- 后续逐步练习后端 API、SQLite 数据库、用户登录和 AI 任务助手。
